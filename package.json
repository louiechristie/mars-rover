{
  "type": "module",
  "devDependencies": {
    "@types/node": "^24.10.0",
    "typescript": "^5.9.3"
  },
  "description": "Code kata based on https://www.codurance.com/katas/mars-rover",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "build": "tsc -w",
    "test": "npm run build & node --watch --test --no-experimental-strip-types"
  }
}
